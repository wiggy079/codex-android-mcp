.DS_Store
node_modules/

# Local credentials and environment overrides must never be committed.
.env
.env.*
.npmrc
*.pem
*.key
*.keystore
*.jks
adbkey
adbkey.pub
local.properties

# Local build/test output. lib/ is produced by `npm run build` on every
# checkout (CI builds it; npm pack runs after the build).
lib/
coverage/
*.log
*.tgz
.tmp-package-verification/

# Local visual acceptance evidence may contain workspace or conversation data.
artifacts/
screenshots/
